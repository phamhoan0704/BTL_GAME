using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;

public class selectLevel_Controller : MonoBehaviour
{
    [SerializeField] GameObject selector;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
    void selectLevel()
    {/*
        if (selector = "level1")
        {
            SceneManager.LoadScene(SceneManager.GetActiveScene("level1"));
        }
        if (selector = "level2")
        {
            SceneManager.LoadScene(SceneManager.GetActiveScene("level2"));
        }
        if (selector = "level3")
        {
            SceneManager.LoadScene(SceneManager.GetActiveScene("level3"));
        }
        if (selector = "level4")
        {
            SceneManager.LoadScene(SceneManager.GetActiveScene("level4"));
        }*/
    }
}
